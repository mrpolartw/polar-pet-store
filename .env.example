# Polar Pet Store 前端環境變數範本
# 複製此檔案為 .env.local 並填入實際值
# 注意：.env.local 不可 commit 到 git

# 後端 API 位址
VITE_API_BASE_URL=http://localhost:9000

# 開發 Mock 模式（true=啟用假資料，false=使用真實後端）
# 正式環境請設為 false 或移除此行
VITE_USE_MOCK=false
